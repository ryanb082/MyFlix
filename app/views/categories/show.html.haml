%section.genre
  %header.clearfix
    %h1
      = @category.name
    .genre_sorting
    Sort by:
    %select
      (name="")
      %option Title: A - Z
      %option  Title: Z - A
      %option  Rating
    %article.row
      - @category.videos.each do |video|
        .video.col-md-2
          = link_to video, nil do
            = image_tag video.small_cover_url

